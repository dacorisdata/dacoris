import en from './en';
import fr from './fr';
import ar from './ar';

// Namespaced translation dictionaries, keyed by locale code.
// Add a new import + entry here when adding a locale.
export const translations = { en, fr, ar };
